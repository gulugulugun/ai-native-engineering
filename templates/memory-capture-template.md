# Memory 沉淀模板

> 只记录长期有效、可复用的结论；不要直接记录一次性聊天内容。
> 按四分类归档，每条经验归入最匹配的一类。

## 分类

选择一个：
- **Pattern** — 可复用的模式和做法（如"分页查询的参数校验逻辑"）
- **Pitfall** — 踩过的坑和需要避免的做法（如"下游 Svrkit 超时需降级处理"）
- **Preference** — 命名偏好、约定（如"查询接口统一用 GET /api/{resource-name}"）
- **Architecture** — 架构决策和技术选型（如"轻量逻辑放 entity/，不加 service 层"）

## 标题

-

## 适用场景 / 触发条件

-

## 结论 / 约束

-

## 证据 / 参考文件

-
