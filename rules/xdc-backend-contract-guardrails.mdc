---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-04-03T09:50:37.358Z
provider: 
---

## 适用范围
`xdc_wxpay` 及同类 XDC Node 后端任务。

## 核心约束
- 接口变更优先修改契约，再生成或对齐代码；不要先手写 Controller 签名再回补契约。
- `controller/` 以契约生成结构为准，开发时主要补业务逻辑，不随意改动生成约束。
- 轻量业务逻辑优先放在 `entity/`，不要无理由再补一层 `service/`。
- 对外依赖、客户端代码生成、模块依赖管理优先通过 `xdc.yml` 和生成工具处理。
- 业务异常优先使用 `BizError` 与标准错误码语义，保持接口返回一致性。

## 评估要求
- 设计或修改后端接口时，同时评估前端契约调用、下游依赖和生成代码影响。
- 复杂能力优先走契约驱动闭环，而不是手写散落的接口调用。