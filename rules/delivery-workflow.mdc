---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-04-03T09:50:37.477Z
provider: 
---

## 默认工作流
- 开始实现前，先读取相关项目规则和必要上下文，再动手修改。
- 先判断当前任务属于 Review-only、Implement 还是 Fast-fix，再决定动作粒度与验证强度。
- 修改代码后，优先检查最近改动文件的 lints / 必要验证，不把验证工作完全留到最后。
- 持久化到 `MEMORY.md` 的内容必须是长期有效、可复用的经验；一次性聊天、临时讨论不要直接沉淀进去。
- 任务跨会话时，使用 handoff 模板记录目标、已完成、风险和下一步。

## 决策偏好
- 方案优先“CodeBuddy 原生能力先行”，避免过早引入额外框架。
- 先做最小可执行版本，再根据真实痛点补更复杂的治理链或自动化。